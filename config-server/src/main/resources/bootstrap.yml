spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Dhiraj2996/EventDrivenMicroservices/tree/main/config-server-repository
          default-label: main
          clone-on-start: true
    fail-fast: true